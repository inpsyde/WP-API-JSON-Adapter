<?php # -*- coding: utf-8 -*-

/**
 * It's just a stub of the original class from the WP-API plugin.
 * The original classes can not be loaded outside an WP environment
 * without producing errors.
 *
 * But to use type-hinting also in Unit-Tests we need this class in the SUT.
 */
class WP_JSON_Server {

	public function dispatch() {}
}